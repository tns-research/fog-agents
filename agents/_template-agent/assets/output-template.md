# <Agent Output Title>

**Project:** <project-slug>
**Date:** YYYY-MM-DD
**Run by:** <agent-name>
**Inputs:** <key inputs as a one-liner>

---

## Summary

3 to 5 sentences. The single most important takeaway first. No hedging, no recap of the inputs.

## Findings

The body of the report. Per-agent: tables / scorecards / scoring / quotes / heuristics. Subsections (`### <name>`) are fine. Quantitative subsections come first, qualitative second.

## Recommendations

3 to 5 actions, ranked by impact. Each: action, rationale, first concrete step.

## Methodology

How the data was gathered (CLIs run, queries used, recency window applied). One paragraph or a short list.

## Limitations

What the run could NOT cover (rate limits hit, sources blocked, time-bounded data, sample size). Be honest.

## Sources

- [Title](URL): what was extracted
- [Title](URL): what was extracted

---

*Generated by `<agent-name>` on YYYY-MM-DD. Founders Growth Agent Stack.*

---

**Conventions for per-agent output templates:**

- Always start with the metadata block, then `## Summary`, then domain content.
- Always end with `## Sources` and the generation footer.
- Per-agent templates may rename `## Findings` into a domain-specific name (e.g. `## Quantitative Data`, `## SERP intent-gap analysis`, `## Channel inventory`) and split it into multiple H2s. `## Summary` and `## Sources` stay literal.
- When the agent emits structured data (rankings, scoring, lists), also write a sidecar `<label>-<YYYYMMDD>.json` next to the .md (cross-cutting C1).
